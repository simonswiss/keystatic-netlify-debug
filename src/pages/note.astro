---
import List from '~/layouts/list.astro'
import { getCollection } from 'astro:content'

export const prerender = true

const posts = await getCollection('blog', ({ data }) => data.note)
---

<List title="Catatan" posts={posts} />
